import { describe, it } from 'vitest';

describe('account number validation (sprouted method)', () => {
  it.todo('collects names of employees whose account number is empty');
  // Example (uncomment and implement test-first):
  // it('returns invalid account holders when account number is empty', () => {
  //   ...
  // });
});

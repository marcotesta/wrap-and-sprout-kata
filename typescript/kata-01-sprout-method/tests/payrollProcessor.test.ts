import { describe, it } from 'vitest';

describe('account number validation (sprouted method)', () => {
  it.todo('rejects an account number that is empty');
  // Example (uncomment and implement test-first):
  // it('rejects an account number that is empty', () => {
  //   ...
  // });
});

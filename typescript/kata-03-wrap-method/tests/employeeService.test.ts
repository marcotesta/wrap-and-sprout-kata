import { describe, it } from 'vitest';

describe('promotion event publishing (wrapped method)', () => {
  it.todo('publishes a PromotionEvent after a successful promotion');
  it.todo('does NOT publish when executePromotion throws');
  // Example (uncomment and implement test-first):
  // it('publishes a PromotionEvent on success', () => { ... });
});
